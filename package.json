{
  "name": "datatrace-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "cd apps/web && npm run dev",
    "dev:api": "cd apps/api && npm run dev",
    "build": "npm run build:web && npm run build:api",
    "build:web": "cd apps/web && npm install --legacy-peer-deps && npm run build",
    "build:api": "cd apps/api && npm install",
    "start": "concurrently \"npm run start:web\" \"npm run start:api\"",
    "start:web": "cd apps/web && npm start",
    "start:api": "cd apps/api && npm start",
    "install:all": "npm install && cd apps/web && npm install && cd ../api && npm install",
    "railway:build:web": "cd apps/web && npm install --legacy-peer-deps && npm run build",
    "railway:start:web": "cd apps/web && npm start",
    "railway:build:api": "cd apps/api && npm install",
    "railway:start:api": "cd apps/api && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}